<div class="container p-2">
  <%= simple_form_for([@user, @couch]) do |f| %>
   <%= f.input :address %>
   <%= f.input :price %>
   <%= f.submit "Add a couch", class:"btn btn-flat" %>
  <% end %>
</div>
